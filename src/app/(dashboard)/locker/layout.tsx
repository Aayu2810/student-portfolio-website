// Locker Layout
export default function LockerLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="locker-layout">
      {/* TODO: Add locker-specific layout/navigation */}
      {children}
    </div>
  )
}
